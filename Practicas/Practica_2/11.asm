
ORG 3000H
CONTAR: INT 7
 MOV AL, 20H
 OUT 20H, AL
IRET

ORG 1000H
CARACTER DB "A"

ORG 2000H
 MOV AX, CONTAR 
 MOV BX, 40 
 MOV [BX], AX 

 
 CLI
 MOV AL, 11111110B 
 OUT 21H, AL 
 MOV AL, 10
 OUT 24H, AL 
 STI
 
 MOV BX, OFFSET CARACTER
 
 REINICIAR: MOV BYTE PTR [BX],40H
 
 LOOP: INC BYTE PTR [BX]
 MOV AL,1
 MOV AH,0
 CMP BYTE PTR [BX],5AH
 JZ REINICIAR
 JMP LOOP 
INT 0
END
